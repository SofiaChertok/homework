// Bugs: Enter this program and compile it. Why does she fail? How can this be fixed?
// Correct the error in exercise 3, then recompile it, compose it and run it.

#include <iostream>
using namespace std;

int main()
{
  cout << "Is there a bug here?";
  // The bug is that the semicolon is missing after the cout statement.
  return 0;
}    
